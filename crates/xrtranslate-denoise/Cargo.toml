[package]
name = "xrtranslate-denoise"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Ultra-lightweight real-time GTCRN speech enhancement and background noise suppression."

[features]
default = []
managed-ort = ["ort/load-dynamic"]

[dependencies]
ndarray = "0.17"
ort = { version = "=2.0.0-rc.13", default-features = false, features = ["std", "ndarray", "tracing", "download-binaries", "tls-rustls", "api-27"] }
tracing = "0.1"

[dev-dependencies]
