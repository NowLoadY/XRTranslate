pub(super) use crate::ui::graph_style::{
    ERROR_BORDER, ERROR_FILL, GRAPH_ACCENT, GRID, LINK_SELECTED, MUTED, NODE_BORDER, NODE_MUTED,
    NODE_TEXT,
};

pub(super) use crate::ui::graph_style::apply;
